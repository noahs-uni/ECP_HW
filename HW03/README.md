**CIFAR-10 Inference** with `hw03_cifar10_inference.py`

+ Place script `hw03_cifar10_inference.py` into `adapt/examples/` and run **inside the Docker image**
+ Place header files into `adapt/adapt/cpu-kernels/axx_mults`
